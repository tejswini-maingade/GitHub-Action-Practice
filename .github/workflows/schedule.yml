name: Scheduled Workflow

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - name: Scheduled job
        run: echo "Scheduled workflow is running"
